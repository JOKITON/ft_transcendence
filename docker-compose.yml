name: ft_transcendence

services:
  proxy:
    container_name: proxy
    build:
      context: ./docker/proxy/
      dockerfile: Dockerfile
    networks:
      - frontend
    env_file:
      - docker/proxy/.env_proxy
    restart: always
    depends_on:
      - app
    ports:
      - "81:81"

  app:
    container_name: app
    build:
      context: ./docker/app/
      dockerfile: Dockerfile
    networks:
      - frontend
      - backend
    env_file:
      - docker/app/.env_app
    restart: always
    depends_on:
      - db

  db:
    container_name: db
    build:
      context: ./docker/db/
      dockerfile: Dockerfile
    networks:
      - backend
    env_file:
      - docker/db/.env_db
    restart: always
    # Volume ensures data persists
    volumes:
      - db-data:/var/lib/postgresql/data

networks:
  frontend:
    driver: bridge
  backend:
    driver: bridge

volumes:
  db-data:
